1. Open the solution in Visual Studio 
2. Build the solution to have the packages restored (build finishes with error)
3. Run make.ps1 to create bin folder with all needed dlls
4. You are ready to go (now builds successfully)
4a. Use clean.cmd to remove packages and bin folders if necessary